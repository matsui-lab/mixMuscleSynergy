% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_mixture_data.R
\name{simulate_mixture_data}
\alias{simulate_mixture_data}
\title{Simulate Mixture Data for MFA/MPCA}
\usage{
simulate_mixture_data(
  N = 50,
  K = 3,
  r = 3,
  M = 8,
  T_each = 100,
  cluster_sep = 1,
  noise_scale = 1,
  seed = 123
)
}
\arguments{
\item{N}{Number of subjects (time-series).}

\item{K}{True number of clusters in simulation.}

\item{r}{True dimension of factors or principal components.}

\item{M}{Number of observed channels (e.g. muscle EMG channels).}

\item{T_each}{Length of each time-series per subject.}

\item{cluster_sep}{How far to separate cluster means (and possibly scale the factor loadings).}

\item{noise_scale}{Scaling of noise diagonal in \code{\link{Psi_list}}.}

\item{seed}{Random seed for reproducibility.}
}
\value{
A list with:
\item{list_of_data}{A list of length \code{N}, each an \code{(T_each x M)} matrix.}
\item{z_true}{A length-\code{N} vector of true cluster assignments (1..K).}
}
\description{
This function generates synthetic time-series data according to a mixture of factor (or PCA) model.
Each subject is assigned to one of \code{K} clusters, each with factor loadings (or principal vectors),
mean offsets, and diagonal noise.
}
\examples{
\dontrun{
sim <- simulate_mixture_data(
  N=50, K=3, r=4, M=8, T_each=100, 
  cluster_sep=1.0, noise_scale=1.0, seed=123
)
length(sim$list_of_data)  # 50
sim$z_true[1:10]
}
}
