% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_parameters.R
\name{simulate_and_run_serialParam_parallelInit_save}
\alias{simulate_and_run_serialParam_parallelInit_save}
\title{Run Simulation (MFA & MPCA) for Each Param in a For-Loop, Parallelizing Over Multiple Initial Seeds}
\usage{
simulate_and_run_serialParam_parallelInit_save(
  K_true_vec = c(3, 4),
  r_true_vec = c(3, 5),
  N_vec = c(50),
  M_vec = c(8, 12),
  T_each = 100,
  sep_vec = c(0.5, 1),
  noise_vec = c(1, 2),
  K_candidates = 1:5,
  r_candidates = 1:5,
  max_iter = 30,
  n_rep_init = 5,
  mc.cores_inits = 2,
  seed_data_base = 2023,
  output_dir = "results",
  ...
)

simulate_and_run_serialParam_parallelInit_save(
  K_true_vec = c(3, 4),
  r_true_vec = c(3, 5),
  N_vec = c(50),
  M_vec = c(8, 12),
  T_each = 100,
  sep_vec = c(0.5, 1),
  noise_vec = c(1, 2),
  K_candidates = 1:5,
  r_candidates = 1:5,
  max_iter = 30,
  n_rep_init = 5,
  mc.cores_inits = 2,
  seed_data_base = 2023,
  output_dir = "results",
  ...
)
}
\arguments{
\item{K_true_vec}{Vector of true K values to simulate.}

\item{r_true_vec}{Vector of true r values to simulate.}

\item{N_vec}{Vector of subject counts.}

\item{M_vec}{Vector of channel counts.}

\item{T_each}{Time-series length.}

\item{sep_vec}{Vector of separation parameters.}

\item{noise_vec}{Vector of noise scale parameters.}

\item{K_candidates}{Vector of candidate K values to try.}

\item{r_candidates}{Vector of candidate r values to try.}

\item{max_iter}{Max iteration for EM.}

\item{n_rep_init}{Number of random initial seeds for each (K,r).}

\item{mc.cores_inits}{Number of parallel cores to use **for initial seeds**.
(パラメータグリッド自体はforループで回すので並列化しない)}

\item{seed_data_base}{Base seed for data simulation.}

\item{output_dir}{Directory to save each result as rds.}

\item{...}{その他、mfa_em_fit_cpp_singleInit() や mixture_pca_em_fit_cpp_singleInit() に渡したい追加引数}
}
\value{
A data frame with columns:
\item{K_true, r_true, N, M, sep, noise}{True simulation parameters.}
\item{ARI_MFA, BIC_MFA, Khat_MFA, rhat_MFA, ARI_PCA, BIC_PCA, Khat_PCA, rhat_PCA}{Results.}

A data frame with columns:
\item{K_true, r_true, N, M, sep, noise}{True simulation parameters.}
\item{ARI_MFA, BIC_MFA, Khat_MFA, rhat_MFA, ARI_PCA, BIC_PCA, Khat_PCA, rhat_PCA}{Results.}
}
\description{
Run Simulation (MFA & MPCA) for Each Param in a For-Loop, Parallelizing Over Multiple Initial Seeds

Run Simulation (MFA & MPCA) for Each Param in a For-Loop, Parallelizing Over Multiple Initial Seeds
}
